rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /songs/{file} {
      allow read: if true;
      allow write: if request.auth.token.email ==
        "kudumulasuryavardhan@gmail.com"
        && file.matches('.*\\.wav');
    }
  }
}
